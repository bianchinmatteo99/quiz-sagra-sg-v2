# TODO

This TODO file was generated by the todo-maintainer agent. It lists actionable, non-speculative tasks discovered in repository comments and documentation. Each item is a single concrete task with the affected file(s) referenced.

**Implementation / Bugs**
- **Complete database-continue restore flow:** In `decideSourceAndLoad()`, handle the `'database-continue'` branch by executing the runtime restore path instead of returning immediately after model creation. Why: without this, continue-from-database cannot reliably resume an in-progress quiz. Affected: `src/common/quiz/quiz.controller.ts`.
- **Implement runtime state rehydration:** Implement `restoreState()` to rebuild in-memory runtime objects from persisted DB state (quiz status, people/ranking context, and active game/question context where applicable). Why: it is explicitly pending and is required by the continue flow and restart resilience. Affected: `src/common/quiz/quiz.manager.ts`, `ARCHITECTURE.md`.
- **Resolve incomplete display subscriptions:** Replace the `"TODO: OTHER BINDINGS?"` placeholder in `DisplayStateHandler.setup()` with explicit additional listeners, or remove the placeholder after documenting why `/state` alone is sufficient. Why: the current uncertainty makes display update coverage ambiguous. Affected: `src/display/display.state.ts`.
