# TODO

This TODO file was generated by the todo-maintainer agent. It lists actionable, non-speculative tasks discovered in repository comments and documentation. Each item is a single concrete task with the affected file(s) referenced.

**Features**
- **Presenter UI:** Implement the planned presenter-facing UI referenced in `README.md`. Why: the project lists a presenter frontend but it is not implemented; adding it completes the set of frontends (user/admin/display) and enables presenter-specific controls. Affected: `README.md`.

**Implementation / Bugs**
- **Implement quiz restore logic:** Complete the restore flow in `decideSourceAndLoad()` where "TODO: Implement restore logic" is present so that the app can correctly continue from a database quiz. Affected: `src/common/quiz/quiz.controller.ts`.
- **Restore runtime models from DB:** Implement `restoreState()` to rehydrate local models from the persisted database state. Why: required for resilient restarts and "continue from database" behavior. Affected: `src/common/quiz/quiz.manager.ts`.
- **Finalize end-of-quiz ranking flow:** Implement the final ranking display and any necessary persistence/update logic marked by "TODO CLASSIFICA FINALE" in `endQuiz()`. Affected: `src/common/quiz/quiz.manager.ts`.
- **Add missing display bindings:** Complete the bindings in `DisplayStateHandler.setup()` where "TODO: OTHER BINDINGS?" appears (person list, answers, evaluation streams, etc.) so the display receives full state updates. Affected: `src/display/display.state.ts`.

**Security / Configuration**
- **Avoid hardcoded admin UID in DB rules:** Replace the hardcoded admin UID rule or rotate it to a role-based rule (for example by checking `auth.token.admin == true`) as noted in `FIREBASEDB.md`. Why: hardcoded admin UIDs are brittle and unsafe for production; using role-based claims improves maintainability and security. Affected: `FIREBASEDB.md`, `database.rules.json`.

